import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.edsuns',
  appName: 'Vite App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
